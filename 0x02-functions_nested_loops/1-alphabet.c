#include <main.h.h>

/**
 * main - Entry point
 *
 * Description: a function that prints the alphabet, in lowercase, followed by a new line
 *
 * Return: 0 on success
 */
int main(void)
{
	return (0);
}
