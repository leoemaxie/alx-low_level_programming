#include "main.h"

/**
 * print_alphabet_x10: prints the english alphabets in lowercase to stdout
 * 10 times
 *
 * Return: Nothing
 */
void print_alphabet_x10(void)
{
	char c;
	int n = 0;

	while (n < 10)
	{
		for (c = 'a'; c <= 'z'; c++)
			_putchar(i);
		_putchar('\n');
		n++;
	}
}
